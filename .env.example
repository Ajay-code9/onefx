# Optional local overrides — copy to `.env` or `.env.local` (Vite loads both).
# Example: Vite-prefixed variables are exposed to client code only when prefixed with VITE_.
# VITE_PUBLIC_API_URL=https://api.example.com
